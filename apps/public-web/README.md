# Public Web

The current root Vite app remains the active public website for now so its UI,
routes, styling, and Lovable history stay stable.

Target app role:
- Public content
- Trader registration
- Trader login entry point
- No protected dashboard data
- Calls the shared backend API under `/api/v1/public`

